============================================================
★★ FINAL 2026-06-07（本号最后一程，已收尾，机器交还）★★
============================================================

> 这是最新状态。下面旧内容（★最新 2026-06-06★ 及更早）保留作查史。
> 接手模型：先读 `routes\_START_HERE.md` → 各项目 `handoff\START_HERE.md`（含同名 FINAL 块）。

【DP — 突破天花板，已完成】
- `ft_gen` 跑完完整 4,000,000 步（692 局，日志确认 "VecEnv done ... PPO checkpoint saved"，干净结束不是崩）。
- 最终 **unlocks 2.8→3.33/10**；6 种机制稳定 98–100% 触发：喝水/进食/造矛/穿衣三件套(草衣+毛皮+穿戴)/睡觉/浇水；整局全存活 0→4。
- 关键方法：合并双冠军示范 BC 出全能底座 `bc_gen.pt`（复活 drink/sleep/grassdress/blueprint/water 5 个死动作），再 PPO 微调（广度 1.5 + 保暖 2.0 + 熵 0.02）= `ft_gen.pt`。
- 还没开的：shelter/播种/盖屋/收成/招随从 —— 这几个在两个老师里都是死动作，奖励点不亮，需各自录示范再 BC（已验证路径，是后续）。collect 在 PPO 里退化回 0。
- checkpoint：`routes\deep_protagonist\runs\ft_gen.pt`(48.8MB) + `bc_gen.pt`(48.8MB)。

【PE — "全面开花"方向证伪了"两堵墙"叙事，密集配置是对的杠杆】
- 关键认知更正：PE 涌现窄 **不是"没激励"**（奖励面早拉满：建造完成 150、联合狩猎 18、信号共注意 1.5、must_build 生死门），**是"够不着"**（资源太稀 600×600 才 80 棵树、建造门槛 8 次搬运）。
- 量出的全机制真相（gen319，48 主角）：投掷 38%（唯一在工作），砍树 0.06%、制造 0.02%、建造 0%、狩猎 0%、通讯 0% —— 几乎全是"狂按点不着"（空按），跟 DP 同型。
- 杠杆 = 纯配置提高"够得着性"（不重编）：seed 50025 树 80→240、工地门槛 8→4、石料 6→3，从 gen159 热启动。**结果全面抬升**：me_cov 7→10/25、best_fit 1700→7194、砍树 22→169、制造 6→19、搬运 26→53、建造完成 0→1、狩猎 0→6。
- 配置文件：`routes\protagonist_ecology\onepot_sD_warm_50025.toml`。

【checkpoint 现状（全停训，GPU 空闲）】
- PE：50021→gen369、50022→gen349、50023→gen379、50024→gen379；50025 无独立 checkpoint，从 `onepot_sD_warm_50002\...\checkpoint_gen159` 热基座重启（密集结果已存 CSV）。
- 8 个红线资产全在：bc_hv2 / bc_gen / ft_bc10 / ft_bc05 / ft_gen / r1_cook_s24 / demos_hv2_s24.bin / demos_cook_s24.bin。

【git】本地 commit `9610db1`（`D:\claude-code\c++` master，无 origin）：handoff 包 + 本轮代码改动 + 50025.toml。

【接手下一步（优先级）】
1. DP：给 shelter/播种/盖屋录各自老师示范 → BC 进 bc_gen 的动作并集 → 再 PPO，目标 unlocks 往 5-6 冲。
2. PE：把 50025 密集配置推广到其它种子（50021-24），让"砍树×建造"覆盖整片铺开，而不是死磕狩猎/通讯。
3. 连接方式见 `03_ssh_remote_access_v2.md`：隧道固定（小米球付费版）、私钥可从 secret `DEVIN_SSH_KEY_SANS` 脚本重建，不用找人拿。
4. VM 专属分析文档已备份到 `D:\claude-code\c++\routes\_from_devin_vm\`（21 份）。

============================================================
（以下为历史内容）
============================================================


# HANDOFF 2026-06-01 (executor 收尾) — 三线交接

---

## ⏩⏩ 2026-06-06 更新（D-128→D-135 + P0 涌现谱 + 交接包就绪）★最新，先读这段★

> 这是目前最新状态，覆盖下面所有更早的块。**接手模型开局只需做一件事**：SSH 上机器后读 `D:\claude-code\c++\routes\_START_HERE.md`（顶层总入口），它会把你导进 `deep_protagonist\handoff\` 和 `protagonist_ecology\handoff\`（本轮新整理的权威交接包，UTF-8 干净、带单命令脚本）。

**这一棒做了什么（DP 主线，D-128→D-135）：**
- **D-133 每局随机出生点**（env 开关 `DP_SPAWN_RANDOM`，默认关＝游戏不变）：解决了"②没机会碰到猎物"——以前固定生在有水有食物的角→永远龟在那；改成每局随机生到全图，于是大量局直接贴到猎物 0m。
- **D-134 两阶段狩猎机制**（env 开关 `DP_HUNT_V2`，默认关）★本轮突破★：猎物平时只走路、主角靠近不再恐慌逃（删掉"12m 炸毛"触发）→ 能正常走到身边下第一矛；被矛扎中才进入~0.35s 冲刺(~2m)然后停下喘→留出补第二矛窗口。主角同时有两档移速：推杆≤0.6 走路、>0.6 冲刺（**没加新动作维度，基线 checkpoint 仍能 resume**）。验证：脚本老师 205 局 kills=139。
- **D-135 BC→PPO 管线**：从会打猎的脚本老师录 90 万步示范(`runs\demos_hv2_s24.bin`)→ BC 克隆出 `bc_hv2.pt`（只学会生存、还不主动出手）→ PPO 微调把狩猎"长出来"。**关键发现：必须从 BC 底座微调**(`bc_hv2.pt`→`ft_bc10.pt` kills 从 0 长出来)；从老冠军 `r1_cook_s24.pt` 微调则纹丝不动（旧策略焊死掰不动）。
- **拆穿一个一直骗人的假指标**：日志里 `kills=+0.0` 是**击杀奖励**（按反-Goodhart 设计本来就是 0），**不是击杀数**！之前所有"kills=0"都被它骗了。新加了真计数 `prey_kills=N`。

**P0 涌现谱 baseline（纯埋点/冻结评测，没动训练逻辑，详见 `handoff\P0_baseline.md`）：**
- **两个系统都坍缩到约 1/3 机制宽度，撞同一面墙。**
- DP：23 个动作只用到 8–10 个；10 里程碑平均只解 2.3–2.9 个；整条科技树(cook/mine/axe/pickaxe/monument)+造屋+种地+驯养全 0%；存活率 0（基本都冻死）；`tendfire` 每局狂按 416 次照样冻死＝"空按"（按了没真取暖）。
- PE：4 核心机制只有"投掷"真能成(19.6%)；砍树/造物/近身狩猎 5–6 万次≈0 成功；生火=0、喝水=0、通讯 signal_response≡0；自带 MAP-Elites coverage 仅 0.2–0.32。
- 共同诊断：不是"没去试"，而是大多数机制**要么机制上够不着(成功率≈0)，要么够得着但没压力非做不可**→各自坍缩到一条最省力活路。根因＝**标量爬坡→单策略坍缩**。

**P1 下一步（方向已想好，待用户拍范围；完整方案 `handoff\训练方案_全面涌现.md`）：** 别再优化单标量，改优化**行为空间覆盖度(Quality-Diversity / MAP-Elites)**；DP 把已实现的狩猎势函数推广成 `phi_repertoire`（奖励"这条命用过几种不同机制"）；PE 升级成真 MAP-Elites + 移植两阶段狩猎 + 给通讯加"非协作不可的任务"。评估口径换成"用到的机制种类数↑/被填满的生态位数↑"。

**连接事实（已与 `03_ssh_remote_access_v2.md` 对齐，纠正两点）：** ①小米球隧道地址**固定不变**(`t1dwvu6bzdng.vip3.xiaomiqiu123.top`)，重启电脑也不换；②SSH 私钥可从 secret `DEVIN_SSH_KEY_SANS` 脚本重建（见那篇 §1），不依赖某台 VM。chisel client 在 Devin VM 重启后会死，按那篇 §3 重连即可。

**git 现状（纠正本号早前误判）：** `D:\claude-code\c++` **是 git monorepo**（master，commits 到 D-131），三个项目在 `routes/` 下共享 master、无 origin remote。工作树有大量 untracked scratch（**勿 `git add -A`**）。本轮交接包放在 `routes\...\handoff\` 与 `routes\_START_HERE.md`（在 repo 内，可选择性 commit）。

---

> 接力第一件事（历史）：读本文件 + PE 笔记 §11 + DP 笔记 D-101→D-104。本棒主线 = PE 转「一锅炖」co-tune + GPU 重构；DP 公平 eval 纠错。所有 commit 仅本地（仓库无 origin remote，天然 local-only），未 push。

---

## ⏩ 2026-06-03 更新（D-122 系统审计 + 寒夜稀缺 + 精确溯源惩罚 + 本号收尾）

> **这是最新状态，优先读这一节**（覆盖下面的 2026-06-02 块）。详见 DP 笔记 D-122 当前状态块 + `docs/机制闭环审计_账本.md`。

**用户两条新指令**：①跑完这轮(修复版 `_e`)就收手；②更新 prompt 文档 + 红线重定义（基本无红线，唯一红线=不许阉割功能/不许乱搞，已写进 DP 笔记的"协作规则/红线"两节）。

**系统审计结论**（用户要的"原始人视角看机制闭不闭环"）：链闭、经济开——基准线吊床(摘果子+窝棚回血=永生零风险)→每级科技白加负担、cook 天生没需求(杀了当场吃饱+40，囤熟肉只多+5)。

**两个根因修复(只加不删、cook_enabled 闸门、v7 字节一致、备份 .bak_econ_20260603)**：①寒夜窝棚不再回饥饿(Environment.cpp L762-785，逼囤熟肉过冬)；②凸的按指标溯源惩罚 event_vital_danger(RewardSystem，coef 0.02，平方亏空→单个垮掉指标罚最重→PPO 梯度+GAE 死亡回溯精确落到放任它的决策串)。验证：build_d122_v2 编译 OK、`--self-test` 两条 vital_danger 单测 PASS、`--cook-selftest` 9/9 PASS。

**当前在跑(本号收尾后挂着，跑完收手)**：PID 32556=修复版血脉 `_e`(从 r1_cook_s24.pt 真续训、critic-warmup64、起步 score0.255~0.264 未塌)；PID 7984=旧经济对照 `_c2`(A/B)。旧 hunt-nudge(PID19956)已杀。脚本 launch_r1_cook_e.ps1。待观察：`_e` kills>0/cook OK 首现 + 跟 `_c2` 差异。

---

## ⏩ 2026-06-02 更新（拟真补全收尾 + 训练已全停）

> 已被上面 2026-06-03 块覆盖；以下为更早历史。完整交接见仓库根 `PROJECT_STATUS.md`。

**训练全部停止**：DP `s5_v3`（pid 38188）已杀；PE 旧世界保活 seed（50012–50017）跑完并连 run 目录一起清理（释放 11.6GB → D: 213.8GB）。**当前无训练在跑**——世界未定稿前继续保活无意义（权重统一重训时作废）。

**本轮做了什么（代码已落盘 + commit `4a69282`，仍仅本地无 origin）**：
- **PE 世界机制抬到 DP 深度**（编译通过，**未重链 exe、未重训**）：① 火=资源(点燃给燃料/每tick烧/风暴3×/烧尽自灭) ② 洞穴热缓冲(~15℃恒温/散热降35%) ③ `IgniteFireCapability`(+1动作，主角点火，闪电自燃率砍到15%) ④ `CavePerception`(+4 obs：是否在洞内+最近洞口方位/距离) ⑤ 洞穴防掠食。③④ 是**破契约**→PE 必须起全新 seed 重进化。
- **DP `phi_hunt_progress` 猎杀势能课程**：已编译（非破契约，可从 s5_v2.pt 热启），**未折入重训**。历史教训：直接给击杀奖励会被 Goodhart（D-064 实测 8%→0%），用 PBRS+收紧替代路径，不发击杀赏金。
- **两个 viewer 拟真**：PE viewer main.js（不规则水盆地+实例化草+生态地形）；**DP viewer 首次入库** `routes/deep_protagonist/viewer/web3d`（斑驳生态地形+34k草+不规则水）。**DP 洞穴渲染未完成**（dome 方案被否，正确做法=移植 PE 的 cell 化小灰岩聚簇 `pe_web3d/src/main.js` L692–725，不抬大knoll/不做dome；`raiseCaveHills` L131-149 抬丘过高需压低）。

**下一步（唯一一次统一重训）**：训练已停 → 可干净重链两个 exe（此前 LNK1104 因 exe 被锁，现已解锁）→ 重 dump（带 PE caves + 建造真进度）→ DP 从 s5_v2.pt 热启切最终 lineage（折入猎杀课程，~2.7h）+ PE 起 3 fresh seed（含点火动作+洞穴obs）。调优方法论=用户「撒米引诱小鸡」：铺连续奖励梯度、每步比旁边香、不留躺平平台、堵空子。

## 仓库结构（关键）
- `D:\claude-code\c++` 是**单一 monorepo**（git root），三个项目在 `routes/` 下，共享同一 master。无 origin remote。
- HEAD 链：`90f2688`(PE long-run config+specs) → `4a27000`(GPU-arch 隔离副本) → `a86b2eb`(DP --domain-rand) → `63f3144`(PE 统一血脉+FOV退火) → `ce7db1e`(thermal0.45 CAPPED 裁定)。
- 工作树残留 = 555 untracked 脚本/build 目录 + 50 deleted 旧 run 产物（**非代码**，勿 `git add -A`，会扫进一堆 scratch）。代码/配置改动已全部 commit。

## 线 1 — PE protagonist_ecology（核心）
**状态**：单轴打法穷尽（thermal0.45=CAPPED，2/3 seed 振幅不破 + 3/3 lead 转负=预判被摧毁）→ 决策者拍板「一锅炖」A-F 全机制整合 co-tune。
**已落地**：统一血脉编码（input_dim 锁满 +11 维，flag 关写零，不再废 checkpoint）+ FOV 退火器（难度=f(代数)，gen0=180°→gen45=70°）+ Phase C FOV 门控（复用 isVisible，看不见火→方位写零留暖意，逼记忆+预判）。Step0+C smoke=PASS。
**在跑**：Step0+C 80代长跑（PID 39264，`routes/protagonist_ecology/onepot_sC_long_50001.toml`，输出 `data/runs/onepot_sC_long_50001`，~gen13/80）。判据：gen60-79 尾段谐波 phase-lead 能否转正。
**下一步**：长跑完出裁定 → D/E/F 给真逻辑 + A 尺度 + co-tune 势函数(PBRS，不发直接奖)。
**红线**：smoke 先验 C7、生产 exe=`neural-eco-protagonist-rhythm.exe`、commit 仅本地、D free ≥200GB。

## 线 2 — PE GPU 推理架构重构（隔离，用户专门点的活）
**状态**：隔离副本 `routes/protagonist_ecology_gpuarch`（与在跑的 PE/DP 完全隔离，源码零改动）。cohort-resident 权重：每 cohort `uploadCohort()` 一次，每 tick `forwardResident()` 只过小张量，消灭每 tick 重传 ~14MB 全权重（=eco 卡 CPU/GPU 欠用根因）。
**验证**：CPU-path parity=PASS（max|Δ|<1e-4 三路一致；H2D 200tick 399MB→12.8MB = −96.8%/31×，2026-06-01 重新确认）。legacy `forward()` 已 dedup 到共享 `launchDenseKernels()`（与 `forwardResident()` 同一发射路径，杜绝漂移）。文档 `GPU_RESIDENCY_REFACTOR.md`，测试 `runtime/test_resident_parity.cpp`。commit `4b930a1`。
**全 CUDA build + GPU 数值 parity = 已完成 PASS（2026-06-01）**：CUDA 工具链其实在 **`D:\NVIDIA\CUDA\v12.8`**（nvcc V12.8.61；之前 C 盘 v11.8/v12.4 是桩、`CUDA_PATH=G:\ai\nvidia` 是死链，所以首轮误判「无 nvcc」）。CUDA 12.8 支持 RTX 5060 的 **sm_120**，首次在真 GPU 上编译运行 kernels。**结果：resident-vs-legacy = 0.000e+00（位级一致）、resident/legacy-vs-cpu = 7.15e-7（远<1e-4 tol）、H2D −96.8%/31.3× → RESULT: PASS**。最小独立编译：`vcvars64.bat` + `nvcc -std=c++20 -DNEURAL_ECO_HAS_PROTAGONIST_CUDA=1 -arch=sm_120 -I<root> test_resident_parity.cpp BatchedDenseGpu.cpp BatchedDenseGpu.cu`。commit `2c3d3f2`。
**顺手修了一个测试 harness bug**：首跑报 1.748 是假象——测试先把 CPU 轨迹跑到底，再拿 GPU 每-tick 状态去比 CPU 的**最终**状态（跨 tick 比较）。改成 CPU 在 tick 循环内 lockstep 推进后 PASS。（也顺带发现：之前「CPU-path parity PASS」是空跑——无 CUDA 时 `supported()`=false，比较块压根没执行。）
**已合回主线（2026-06-02，commit `591841b`）**：逐文件搬 7 文件进 `routes/protagonist_ecology`（+617/−63），CPU build 不回归、真 GPU parity 仍 PASS（resident==legacy 0.000e+00、vs CPU 7.15e-7、H2D −96.8%）；主线一锅炖/FOV/Phase C 未被动，DP 未碰。

## 线 3 — DP deep_protagonist（§4 营养 + 公平 eval 纠错）
**状态**：§4 营养修复有效（oracle 64-seed protein 仅 4 例，营养已非杀手）。但 PPO 学出来的策略 held-out **0% 存活**，根因=**学不会蛋白觅食的 learning gap**（非 horizon、非世界难）。
**决定性数据（fair 协议 1ep×9000×32 held-out seed，protein40）**：settler oracle **47%**(protein饿死0) / PPO v2 **0%**(protein25) / PPO v3 域随机化 **0%**(protein21)。→ **域随机化单独不够**。
**关键纠错**：(1) episode cap=9000 步 ≥ protein 死时标 4740 步，horizon 不是问题；(2) `--episodes 8` eval 协议被污染（鱼只在建图时 relocate，后续 episode 不重定位 → 第 2+ 局蛋白饿死），oracle 同协议也只 8% → 该协议无效，之前 v3 "2%" 作废。**eval 一律 fair 协议，认 oracle≈47% 为可生存基线。**
**下一步 v4（让蛋白觅食可学，按优先）**：①BC base 演示叉鱼觅食 ②PBRS 低蛋白时引导趋水/鱼(不发直接奖) ③加大 entropy ④保留域随机化。
**注**：DP=单个原始人，无社会/季节。剩余蓝图 = §4 gate(本棒)→W1 天气/雨灭火/保暖冗余→W2 耦合网→§5 立新 BC base+KL退火→§6 多轴泛化门。
**复现 fair eval**：`build\bin\deep_protagonist_train.exe --policy ppo --load runs\<ckpt>.pt --no-update --nutrition --nutri-protein-decay 40 --ppo-mask-fire-only --steps 9000 --episodes 1 --seed <3000000..3000031> --metrics runs\fair_x\<seed>.jsonl`（批脚本 `tmp_s4\fair_v2.bat / fair_v3.bat`）。

## 待补（两轮跑完后）— 进展
- [完成] PE Step0+C 80代长跑 gen60-79 谐波最终裁定 → 已写入 PE 笔记 **§11.FINAL**。结论 = **半破(1-seed 初判)**：趋火相位 lead 全程转正 **+5..+17s**（3/5 LEAD、0/5 反应；与 thermal0.45 的 −17..−31s 正相反 → Phase C FOV 门控保住「预判方向」）；但振幅 amp(0.006-0.008)+R²(0.05-0.10) 仍低于闸(0.009/0.13) → **振幅平台未被 C 单机制击穿**。含义：C 给方向、给不了幅度，需 co-tune A(尺度→路程延迟)+E(预备建造→提前回报) payoff 轴顶振幅。**红线：仅 seed 50001 单 seed，需 50002/50003 3-seed 复核**（相位定性结论比 amp 数值可信）。
- [完成] GPU-arch 全 CUDA build + parity：在 RTX 5060 / CUDA 12.8(`D:\NVIDIA\CUDA\v12.8`) 上跑通，resident==legacy 位级一致、vs CPU 7.15e-7、PASS（详见线 2）。merge 回主线已完成（commit `591841b`，2026-06-02）。

---

## 速度实测（2026-06-01 追加）— GPU-arch 加速到底多少
实测于真 RTX 5060 / CUDA 12.8（micro-bench，forward 热路径）：

**GPU 内部（legacy 每tick重传 vs resident 只传一次）：**
- parity 维（权重 1.9MB）：1.057 → 0.336 ms/tick = **3.15×**
- 生产维（权重 21.3MB）：3.68 → 0.73 ms/tick = **5.04×**（权重越大省越多）

**CPU vs GPU（真实主角维 B=24 / in=272 / hid=512 / act=13 / iface=360，权重 31.8MB）：**
- CPU 9.10 ms/tick ← **在跑的 PE 实际走的路径**（CUDA 从没编进那 exe）
- GPU legacy 5.06 ms/tick；GPU resident 0.62 ms/tick
- **CPU→resident = 14.76×**；legacy→resident = 8.20×
- （注：parity 小维 CPU→resident 仅 0.77×，GPU 输给 CPU——kernel 启动开销 > 琐碎计算；只有 hidden=512 真实规模才赢。）

**per-gen 折算（旧估算，已被下方「速度实测 v2」真实端到端取代）：**
- ~~每代稠密推理只占 ~5–7%，单这重构每代只快几%~~ —— 此估算基于错误假设「1 主角 × 1200 tick」，但每代实际评估 **24 主角 × 2 episode**，前向调用量高一个量级。真数见下方 v2 节。
- 数据全文：`D:\claude-code\c++\routes\protagonist_ecology_gpuarch\GPU_RESIDENCY_REFACTOR.md`（commit `6885188` / `19e436c`）。

## ★速度实测 v2（2026-06-02，真实端到端 in-sim，权威）★
方法：同机 RTX 5060 / 同种子 50001 / 同 onepot config，仅改后端，各跑 6 代，读内置 `Gen N profile` 行的 `gen_total`（每代墙钟）。三种后端：逐agent CPU（生产默认 batched=false）/ batched-CPU（batched 开 + `force_cpu=true`）/ batched-GPU（batched 开 + GPU，nvidia-smi 确认 rhythm.exe 在 GPU 上）。

| 代 | 逐agent CPU | batched-CPU | batched-GPU | GPU vs 逐agent |
|---|---|---|---|---|
| 0 | 20.5s | 22.0s | 12.7s | 1.62× |
| 1 | 22.6s | 24.1s | 15.7s | 1.44× |
| 2 | 24.8s | 27.5s | 19.7s | 1.25× |
| 3 | 27.6s | 31.4s | 23.7s | 1.17× |
| 4 | 33.0s | 36.4s | 28.5s | 1.16× |
| 5 | 38.3s | 42.5s | 35.8s | 1.07× |
| 6代合计 | 166.8s | 184.0s | 136.1s | **1.23×** |

**brain 前向块（2 线程累计秒）**：逐agent CPU ~15s · batched-CPU ~19s · **batched-GPU ~3s**。

**结论（实测，权威，取代上面 micro-bench 的 per-gen 估算）：**
1. **加速全部来自 GPU，不来自 batching。** batched-CPU 比逐agent 还略慢（多 marshalling 开销、无 GPU 收益）→ 生产里**别开 batched-CPU**；要开就 batched+GPU。brain 前向 GPU 真实 **~5–6×**（~15s→~3s）。
2. **整代墙钟只快一点且递减**：gen0 1.62× → gen5 1.07×。原因 = world_tick（资源/水/篝火/狩猎，未上 GPU）占比大且随代数增长（gen0 ~74% → gen5 ~90%），把 brain 从 ~23% 稀释到 ~9%（Amdahl）。
3. **外推 80 代**：world 越来越主导 → 整体加速趋向 **~1.05–1.1×**，不是几倍。要拿几倍必须把 world_tick 也搬 GPU（月级工程）或大幅加大 batch（现 24 agent 喂不饱 GPU）。
4. **build_ninja_cuda 已配成真 GPU build（CUDA-ON, sm_120）**，之后跑 GPU 直接可用；隔壁生产 PE 长跑因 batched 关着仍走 CPU、行为不变。

## merge 安全性结论（用户 Q1，已核实）
- `BatchedDenseGpu` 只被 `protagonist_ecology` 用；`deep_protagonist`(DP) **完全不引用**（DP 跑 LibTorch 自带 CUDA 运行时，与这套物理隔离）。
- 顶层 `CMakeLists.txt` 只 add `src` / `routes/protagonist_ecology` / `tests`，**不含 DP**（DP 独立 build），也**不含 gpuarch**（隔离副本不冲突）。
- 两项目真正共享的是 `src/` 核心库；本重构 **零改动 src/**。
- → **merge 只影响 PE，DP 不会出问题。**
- ⚠ gpuarch 是早先拷的副本，主线 PE 后来加了一锅炖/FOV/Phase C；merge **只能挑文件搬，不能整目录覆盖**（否则冲掉一锅炖）。

## ★接手 agent 待办（按优先，全可执行）★

### TODO-1 ✅已完成（2026-06-02）profile 真实 sim → 精确前向占比 / 每代实省
**结果见上「速度实测 v2」**：brain 前向占每代 ~9–23%（随代数下降，非旧估的 5–7% 也非 40%），整代 GPU 提速 1.07–1.62× 递减。下面是原始方法记录（已执行完）：
1. 在 `D:\claude-code\c++\routes\protagonist_ecology\runtime\ProtagonistBatchedDenseRunner.cpp` 的稠密 forward 调用处包一对 `std::chrono::steady_clock` 计时器累加 forward 总耗时；在进化主循环（`ProtagonistEcologyRuntime`，约 L4256 盖章代数那一带）包 tick 总耗时；跑完打印 `forward_ms / total_ms`。
2. CPU build 现有 eco 目标（生产 exe=`neural-eco-protagonist-rhythm.exe`）。profile 用短 smoke：复制 `routes/protagonist_ecology/onepot_sC_long_50001.toml` → 改 `generations = 2`，跑一遍即可（mainline 暂无独立 smoke toml）。
3. 读 forward 占比 f → 每代实省 = f × (1 − 1/14.76) × 每代时间。
4. **跑完 `git restore` 还原插桩**（不污染主线）。
预算：CPU build + 短 smoke ≈ 15–25 min。

### TODO-2 ✅已完成（2026-06-02，commit `591841b`）精准 merge GPU-arch 回主线 PE
**逐文件 diff 搬，勿整目录覆盖**。从 `routes/protagonist_ecology_gpuarch/` → `routes/protagonist_ecology/`：
- `brain/BatchedDenseGpu.cpp` / `.cu` / `.h`（cohort-resident: uploadCohort/forwardResident + launchDenseKernels dedup）
- `runtime/ProtagonistBatchedDenseRunner.cpp` / `.h`（cohort 指纹 + 驻留 runner）
- `CMakeLists.txt`（仅 GPU-arch 相关增量，比对 diff，别覆盖一锅炖的改动）
- `runtime/test_resident_parity.cpp`（新增测试）
merge 后：CPU build 不回归 + CUDA build 跑 `test_resident_parity` 确认仍 PASS；再确认主线一锅炖/FOV/Phase C 文件未被动。
CUDA 编译参考：`vcvars64.bat` + `nvcc -std=c++20 -DNEURAL_ECO_HAS_PROTAGONIST_CUDA=1 -arch=sm_120 -I<root> test_resident_parity.cpp BatchedDenseGpu.cpp BatchedDenseGpu.cu`。

### TODO-3（可选）PE 3-seed 复核
验证 Step0+C 的正 lead +5..+17s 不是单 seed 假象：
- 复制 `onepot_sC_long_50001.toml` → `_50002.toml` / `_50003.toml`，仅改 seed 字段（mainline 尚无这两个文件，需新建）。
- 各跑 80 代长跑，比对 phase-lead 符号 + amp/R²；3/3 一致才认定性结论。
- 预算：每代 2.5–4 min × 80 ≈ 3–5 小时/seed。

## 关键绝对路径（你机器上）
- 交接文档（本文件）：`C:\Users\sans\Desktop\agent\prompt\HANDOFF_2026-06-01.md`
- PE 知识笔记（§11 一锅炖 + §11.FINAL）：`C:\Users\sans\Desktop\agent\prompt\protagonist_ecology_knowledge_note_2026-05-30.md`
- DP 知识笔记（D-101→D-104）：`C:\Users\sans\Desktop\agent\prompt\deep_protagonist_knowledge_d100.md`
- DP 蓝图：`C:\Users\sans\Desktop\agent\prompt\deep_protagonist_blueprint_world_coherence.md`
- monorepo git root：`D:\claude-code\c++`（无 origin remote = 天然 local-only）
- GPU-arch 隔离副本 + 重构文档：`D:\claude-code\c++\routes\protagonist_ecology_gpuarch\`（文档 `GPU_RESIDENCY_REFACTOR.md` 含全部加速数 + 待办）
- 主线 PE：`D:\claude-code\c++\routes\protagonist_ecology\`
- DP：`D:\claude-code\c++\routes\deep_protagonist\`
- CUDA 工具链：`D:\NVIDIA\CUDA\v12.8`（nvcc V12.8.61，支持 sm_120）
- MSVC：`C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\VC\Auxiliary\Build\vcvars64.bat`
