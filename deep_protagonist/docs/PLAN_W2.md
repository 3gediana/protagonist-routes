# 计划 · Stage W2 耦合网 + D-122 编排（执行者备忘，D-121 跑中拟）

## 现状（D-121）
- W1 天气耦合已上线并训练中：雨灭火 / 湿增寒 / 雨补水 / 保暖冗余(沿用衣+庇护)。
- 顺带闭合：天气→雨→water_level→作物生长→harvest→eat_vitamin（Environment.cpp:706/741-745 已有），即"农耕=可再生维生素"和"天气↔农耕↔维生素"机制上已通。
- obs 569→573（+雨强571 +湿度572，零填充热启动）。从 s5_v2.pt 续训，weather ON + hunt-shaping0.05，48M 步。

## 缺口（按价值/风险，下一步候选）
1. **观测性（先补，零机制风险）**：metrics 无天气埋点。给 main_train.cpp 加 `rain_frac`、`wet_avg`、`deaths_cold`(湿致冷死) 三个统计，才能按 §6 验收门"天气因果可证 / 致死可归因"。⚠ 需重建 exe → 必须等 D-121 跑完（Windows 锁定运行中的 .exe）。
2. **W2-烹饪 cook**：生肉回 protein 少、火上烤熟回满 → 火↔狩猎↔营养。**无需新 obs**（靠 protein 条+火感知）。中等工。
3. **W2-矛⊗狩猎**：craft_spear 提命中/伤害 → 制造↔狩猎↔蛋白。优先复用"已解锁 spear"的现有 craft obs，**免再做血脉手术**；不够再加 1 维。
4. **W2-雨促采集**：野生植物再生速率随雨/雨后加快 → 天气↔觅食↔维生素。小工，无 obs。
5. **§4 天气版生存性 gate**：训冠军前必须先证"会打猎+觅食+护火避雨能活、淋雨躺平会死"（多 seed），否则 NO-GO。

## D-122 编排（铁律：后台时刻有有效训练，GPU 不空转）
- **桥接续训（已备 launch_w1b.ps1）**：D-121 一结束→`prune_runs.ps1`→立刻用 **w1_v1.pt**(机制完整、已训权重) 换 seed 起一个 weather 续训占住 GPU。权重统一、机制完整=有效。
- 并行（离线、不占 GPU）：写 W2 机制 code + 隔离单测（libtorch-free，仿 test_weather.cpp）→ compile 验证。
- D-121 结束后窗口期：重建 exe（含观测埋点 + 选定的 1~2 条 W2）→ 4 行为 smoke → 若改 obs 走 warmstart surgery（新列补零）→ 切 D-122-W2 正式跑，停桥接。

## 红线核对（同蓝图§5）
- 机制只改体征/生存概率间接驱动，**绝不给动作直接发奖**；塑形仅 PBRS。
- 改 obs = 新血脉：旧列不动、新列补零、KL-ref 同步。
- bc_v9/c1 永久保留；新世界冠军独立血脉。
- 致死链留保暖冗余+grace，防来不及学就死光。
- D 盘 ≥200G，smoke run 用完即清。
