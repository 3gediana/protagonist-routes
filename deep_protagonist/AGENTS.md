# AGENTS.md · deep_protagonist

> AI 协作契约。新会话先读。违反 = 失职。

## 用户画像
- 大一，零 RL/DL 基础，不读代码
- 看效果不看术语
- 时间/资源充足，RTX 5060 Laptop 8GB
- 痛点：上个项目"决策完不知道决定了啥"，不能重演
- C++ 课程设计，做到极致

## 决策权与沟通（D-122，2026-06-03 用户重定义）
- **AI 有这个项目的完全决策权**：技术细节、方向、阶段切换、范围、参数全自己拍——不必问、不必等签字、不必复述、不必凑 500 字报告、不必问"开始执行吗"。用户原话："你有这个项目的完全决策权""别问我，自己决策"。
- **唯一保留的沟通习惯**（不是红线，是为用户好）：第一次出现的 AI 术语，当场一句大白话 + 入 `docs/术语速查.md`（用户零基础、看效果不看术语）。
- 旧的"决策必 500 字 / 必复述 / 必问开始执行吗 / 未问不许改方向"——已被用户撤销，作废。

## 时间换算（重要）
- AI 写 30 分钟代码 ≈ 程序员 1 个月工作量
- 估"多久"时分开报：**开发时长（AI 算）** + **训练时长（GPU 算）**
- 别用"程序员月"做估算依据，那是过时的

## 文档写作规则
- 文档 = 写给未来 AI 看的备忘
- **精简，只写核心，不写解释、示范、铺垫**
- 例外：4 份速查文档要大白话（用户会翻）

## 4 份速查（会话结束必更）
- `docs/术语速查.md` — 术语 → 大白话
- `docs/参数速查.md` — 参数 → 作用/调大调小后果
- `docs/决策记录.md` — 用户拍板 → 选了啥/为啥/影响
- `docs/Q&A速查.md` — 用户问过的

## 会话结束自检
- [ ] 新术语都进速查
- [ ] 新参数都进速查
- [ ] 用户决策都进记录
- [ ] 用户问题都进 Q&A
- [ ] `docs/PROGRESS.md` 更新

任一 ✗，不能结束。

## 决策权（D-122：已升级为完全决策权）
- 技术细节 / 方向 / 审美 / 阶段切换 / 范围 / 参数 → **全 AI 自己拍**
- 仅以下仍知会用户（不是审批）：要钱/要密钥、最终交付、用户喊停

## 技术栈
- C++ 17/20 + libtorch + CUDA 12.x
- PPO + Transformer/RNN，5-60M 参数起步
- 真 3D 世界（高度/深度/洞穴）
- raylib 渲染
- 单体超强 agent + 简单陪衬

## 训练铁律（防灾难性遗忘）
- **同一个神经网络从 S4 训到 S6，参数永不重置**
- **每加新任务，旧奖励权重必须保留**（不删、不归零）
- 网络输出包含所有动作（基础+工具+建造），即使早期没用
- 训练曲线分别记录每种行为得分，任一退化立刻警报
- 违反任一条 = 失职

## 红线（D-122 重定义，2026-06-03）
用户原话："基本上没有什么红线，唯一的红线可能就是不能够让你们去阉割功能去乱搞"。

- 🔴 **唯一的真红线 = 不许阉割功能、不许乱搞。** 具体化：
  - ❌ 把功能做成假占位 / 填零，却汇报成"已做"（要么真做 + 自测验因果，要么诚实说没做）
  - ❌ 为指标刷分（用 PBRS 势函数那种最优解上不可刷的，不用"按一下就给分"的直奖）
  - ❌ 重置神经网络 / 删旧奖励 / 破坏老行为（见下"训练铁律"）
  - ❌ 擅自偏离 `docs/最终蓝图.md` 的"不做的事"清单（乱加被砍掉的大系统）
- 已作废的旧红线：决策必 500 字 / 必问"开始执行吗" / 必复述 / 未问不许改方向算法。用户已给完全决策权。
- 仍保留的文档习惯（非红线）：术语必解释、速查别用术语解释术语、别写啰嗦文档。

## 可视化禁令（D-038，2026-05-24）

- ❌ **不要主动加任何 HUD / mini-map / debug overlay / 视觉化**
- ❌ 包括但不限于：SpatialMemory 网格小地图、60m 视野圈、obs 实体高亮线、GRU 内部状态条、训练曲线图
- ✅ 这些都是必做的，但**全部延后**到训练完毕、接口冻结后再统一做
- ✅ 完整待办清单见 `docs/待办_视觉化与打磨.md`
- ✅ 触发条件（任一）：mean reward > +50 稳定 / 用户明确说"开始做答辩" / 进 S6
- 用户原话："如果你现在做可视化，那么以后新增了某些字段啊，那么你那个 3D 也得继续改…放最后面就好了"

## Tool 行为规则（token 经济学）

- **shell context 缓存 5 分钟**。开 background 训练 / 长任务后，**polling 间隔必须 ≤ 4 分钟**，超时缓存失效要重读 context，烧钱
- 训练命令一律 `run_in_background: true`，主 turn 立刻返回 shell_id
- 然后在每 ≤ 4 分钟内做一次 `get_output` poll；间歇期间可以做：写文档 / 改其他文件 / 准备分析脚本（让等待不浪费）
- exec timeout 也别超过 4 分钟同理

## 鏃ュ織/浜х墿鏀跺彛瑙勫垯锛圖-072锛?026-05-28锛岀鐩樼偢杩囦竴娆★級

**瑙﹀彂鍘熷洜**: 鐢ㄦ埛 D 鐩?AI 璁粌鐩稿叧鏁版嵁鏇剧疮璁?~160GB 閲忕骇锛沝eep_protagonist 鑷韩涓€鏅氳窇 10 涓?D-XXX 涔熶細鐣欏嚑鍗佷釜 .opt + .pt + log銆傞渶瑕佹瘡涓?D-XXX 璁畬鑷姩 prune銆?
### 姣忎釜 D-XXX 璁畬蹇呭仛锛堝湪 git commit 涔嬪墠锛?
1. **璺?prune 鑴氭湰**: \powershell scripts\prune_runs.ps1\
   - 淇濈暀: 鏈€杩?5 涓?D-XXX 鍏ㄩ儴浜х墿 + 閲戞爣鍑?ckpt (D-061, D-065)
   - 鍒犻櫎: 鍏朵粬 D-XXX 鐨?.pt / .opt / .jsonl / .stdout.log / .stderr.log
   - 涓嶅姩: 浠讳綍 *.opt.bak* 鏂囦欢 (浜哄伐 hide)
2. **涓嶈鎶?runs/ 鍏?git** (.gitignore 宸插寘鍚?
3. **涓嶈鍦?c++\data\runs\ 鎴?c++\tmp\ 涓嬭緭鍑?deep_protagonist 鏃ュ織** - 杩欎袱涓洰褰曟槸 protagonist_ecology 鐨勶紱娣风敤浼氳鐢ㄦ埛鍒嗕笉娓呰皝鐨勬棩蹇?
### 绂佹

- 鉂?闀挎湡淇濆瓨鎵€鏈?D-XXX 鐨?ckpt (鍏?30 涓氨 120 MB .opt + 60 MB .pt)
- 鉂?璁粌鏃舵妸鏃ュ織杈撳嚭鍒?\c++\tmp\\\ 鎴?\c++\data\\\ (閭ｆ槸鍒殑椤圭洰鍦扮洏)
- 鉂?鍦?docs/ 涔嬪鐨勪綅缃啓姘镐箙鎬?.md (闄?README/AGENTS/.gitignore 绛?

### 閲戞爣鍑?ckpt 缁存姢

- "閲戞爣鍑?鍦?\scripts\prune_runs.ps1\ 鐨?\-goldKeep\ 鍙傛暟閲?- 鍑虹幇鏂伴噾鏍囧噯 (e.g. D-072 score > 0.30) 鈫?鏇存柊鍙傛暟 + 鍦?PROGRESS.md 鏍囨敞
